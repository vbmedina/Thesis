import pandas as pd
import matplotlib.pyplot as plt
import numpy as np

df = pd.read_csv("./pairings_center.csv", usecols=['Strain', 'Chemical', 'Representative_pIC50'])

# # Turn the text labels into integer codes
# df['chem_idx']   = pd.factorize(df['Chemical'])[0]     
# df['strain_idx'] = pd.factorize(df['Strain'])[0]        

# # Hexbin on the codes, colour by max pIC50 in each cell
# fig, ax = plt.subplots(figsize=(7, 6), dpi=300)
# hb = ax.hexbin(df['chem_idx'], df['strain_idx'],
#                C=df['Representative_pIC50'],
#                gridsize=200, reduce_C_function=np.max,
#                linewidths=0, cmap='turbo')
# fig.colorbar(hb, label='max pIC₅₀')

# # Labelse
# xticks = ax.get_xticks()[::2]                    
# ax.set_xticks(xticks)
# ax.set_xticklabels(df['Chemical'].iloc[xticks].values, rotation=90, ha='right')

# yticks = ax.get_yticks()
# ax.set_yticks(yticks)
# ax.set_yticklabels(df['Strain'].iloc[yticks.astype(int)].values)

# ax.set_xlabel('Chemical ID')
# ax.set_ylabel('Strain ID')
# fig.tight_layout()
# fig.savefig('hexbin_all_pairs.png', dpi=600)

print("rows:", len(df), "unique chemicals:", df['Chemical'].nunique(), "unique strains:", df['Strain'].nunique())
